from django.apps import AppConfig

# Configuración de la aplicación de pedidos para el proyecto Django.
class PedidosAppConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'pedidos_app'
